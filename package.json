{
  "name": "storage-browser-demo",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@aws-amplify/ui-react": "^6.0.0",
    "@aws-amplify/ui-react-storage": "^3.0.0",
    "aws-amplify": "^6.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws cloudformation deploy --template-file cloudformation-template.yaml --stack-name storage-browser-demo --capabilities CAPABILITY_IAM && node update-env.js"
  }
}